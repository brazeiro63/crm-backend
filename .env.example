# Database
DATABASE_URL="postgresql://user:password@postgres:5432/crm?schema=public"

# Redis
REDIS_URL="redis://redis:6379"

# JWT
JWT_SECRET="your-super-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# App
NODE_ENV="development"
PORT=3001

# CORS
FRONTEND_URL="http://localhost:3000"
FRONTEND_URL_PROD="https://contratos.casasdemargarida.com"

# Stays API (os valores abaixo alimentam os Docker secrets stays_login/stays_password)
STAYS_API_URL="https://api.stays.com.br"
STAYS_LOGIN="your-stays-login"
STAYS_PASSWORD="your-stays-password"
